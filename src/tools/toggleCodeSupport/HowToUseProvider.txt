Copy to the top of the file and change "HOOKNAME":

import { ToggleCodeProvider } from "../../tools/ToggleCodeContext";
import NotenameButton from "./HOOKNAME/notes/NotenameButton";

Copy to the ParentApp's return():

<ToggleCodeProvider>
    <NotenameButton />
</ToggleCodeProvider>

Copy folder "notes" to HOOKNAME folder